apiVersion: v1
kind: Service
metadata:
  name: rollout-controller
  namespace: naomi-llmd-rl
spec:
  selector:
    app: rollout-controller
  ports:
    - name: http
      port: 8090
      targetPort: 8090
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: rollout-controller
  namespace: naomi-llmd-rl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rollout-controller
  template:
    metadata:
      labels:
        app: rollout-controller
    spec:
      imagePullSecrets:
        - name: ghcr-creds
      containers:
        - name: controller
          image: ghcr.io/naomieisen/llm-d-rl-controller:testing
          args:
            - "--port=8090"
            - "--engines=http://vllm-engine-0.vllm-engine.naomi-llmd-rl.svc.cluster.local:8000"
            - "--health-check-interval=15s"
          ports:
            - containerPort: 8090
          readinessProbe:
            httpGet:
              path: /v1/health
              port: 8090
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /v1/health
              port: 8090
            initialDelaySeconds: 10
            periodSeconds: 30
          resources:
            requests:
              cpu: "500m"
              memory: "256Mi"
            limits:
              cpu: "1"
              memory: "512Mi"
