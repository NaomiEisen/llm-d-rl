# veRL PPO training config — standard vLLM rollout (no llm-d)
#
# Identical to ppo_llmd.yaml in all hyperparameters.
# The only differences are:
#   - No agent_loop_manager_class  → veRL's built-in vLLM rollout
#   - No checkpoint_manager_class  → veRL's built-in weight sync
#   - No checkpoint_engine llmd backend
#   - gpu_memory_utilization set to a real value (not 0.0)
#   - load_format = auto (actually loads weights)
#   - use_legacy_worker_impl = auto
#
# Usage (from the verl repo root, run on KubeRay head pod):
#   python -m verl.trainer.main_ppo \
#       --config-path /path/to/llm-d-rl/deploy/verl \
#       --config-name ppo_vanilla \
#       actor_rollout_ref.model.path=Qwen/Qwen2.5-0.5B-Instruct \
#       data.train_files=/tmp/verl/data/gsm8k/train.parquet \
#       data.val_files=/tmp/verl/data/gsm8k/test.parquet

hydra:
  searchpath:
    - file:///opt/verl/verl/trainer/config
  run:
    dir: /tmp/verl/hydra_outputs

defaults:
  - ppo_trainer   # base veRL PPO config
  - _self_        # this file's overrides win

# ──────────────────────────────────────────────────────────────────────────────
# Data
# ──────────────────────────────────────────────────────────────────────────────
data:
  tokenizer: null
  train_files: /tmp/verl/data/gsm8k/train.parquet
  val_files: /tmp/verl/data/gsm8k/test.parquet
  prompt_key: prompt
  max_prompt_length: 512
  max_response_length: 512
  train_batch_size: 256
  return_raw_input_ids: False
  return_raw_chat: False
  shuffle: True
  seed: 42

# ──────────────────────────────────────────────────────────────────────────────
# Actor / Rollout / Ref
# ──────────────────────────────────────────────────────────────────────────────
actor_rollout_ref:
  hybrid_engine: True

  model:
    path: Qwen/Qwen2.5-0.5B-Instruct
    external_lib: null
    override_config: {}
    enable_gradient_checkpointing: True
    use_remove_padding: False

  actor:
    strategy: fsdp
    ppo_mini_batch_size: 64
    ppo_micro_batch_size_per_gpu: 4
    use_dynamic_bsz: False
    ppo_max_token_len_per_gpu: 16384
    grad_clip: 1.0
    clip_ratio: 0.2
    entropy_coeff: 0.0
    use_kl_loss: False
    ppo_epochs: 1
    shuffle: False
    ulysses_sequence_parallel_size: 1
    optim:
      lr: 1e-6
      lr_warmup_steps: -1
      lr_warmup_steps_ratio: 0.0
      min_lr_ratio: null
      lr_scheduler_type: constant
      total_training_steps: -1
    fsdp_config:
      wrap_policy:
        min_num_params: 0
      param_offload: False
      optimizer_offload: False
      fsdp_size: -1

  ref:
    fsdp_config:
      param_offload: False
      wrap_policy:
        min_num_params: 0
    log_prob_micro_batch_size_per_gpu: null
    log_prob_use_dynamic_bsz: ${actor_rollout_ref.actor.use_dynamic_bsz}
    log_prob_max_token_len_per_gpu: ${actor_rollout_ref.actor.ppo_max_token_len_per_gpu}
    ulysses_sequence_parallel_size: 1

  rollout:
    name: vllm
    mode: async
    dtype: bfloat16
    # Actual GPU memory for vLLM — adjust if OOM (actor + vLLM share one GPU)
    gpu_memory_utilization: 0.6
    load_format: auto
    tensor_model_parallel_size: 1
    max_num_batched_tokens: 8192
    max_num_seqs: 256
    temperature: 1.0
    top_k: -1
    top_p: 1.0
    response_length: ${data.max_response_length}
    prompt_length: ${data.max_prompt_length}
    do_sample: True
    n: 1
    log_prob_micro_batch_size_per_gpu: 4
    log_prob_use_dynamic_bsz: ${actor_rollout_ref.actor.use_dynamic_bsz}
    log_prob_max_token_len_per_gpu: ${actor_rollout_ref.actor.ppo_max_token_len_per_gpu}
    disable_log_stats: True
    enable_chunked_prefill: True
    free_cache_engine: True

    val_kwargs:
      _target_: verl.workers.config.SamplingConfig
      temperature: 0.0
      top_p: 1.0
      top_k: -1
      n: 1
      do_sample: False

# ──────────────────────────────────────────────────────────────────────────────
# Critic
# ──────────────────────────────────────────────────────────────────────────────
critic:
  strategy: fsdp
  optim:
    lr: 1e-5
    lr_warmup_steps_ratio: 0.0
    min_lr_ratio: null
    lr_scheduler_type: constant
    total_training_steps: -1
  model:
    path: ${actor_rollout_ref.model.path}
    tokenizer_path: ${actor_rollout_ref.model.path}
    override_config:
      num_labels: 1
      architectures:
        - Qwen2ForTokenClassification
    external_lib: ${actor_rollout_ref.model.external_lib}
    enable_gradient_checkpointing: True
    use_remove_padding: False
    fsdp_config:
      param_offload: False
      optimizer_offload: False
      wrap_policy:
        min_num_params: 0
      fsdp_size: -1
  ppo_mini_batch_size: ${actor_rollout_ref.actor.ppo_mini_batch_size}
  ppo_micro_batch_size_per_gpu: 4
  use_dynamic_bsz: ${actor_rollout_ref.actor.use_dynamic_bsz}
  ppo_max_token_len_per_gpu: 32768
  ppo_infer_max_token_len_per_gpu: 8192
  ulysses_sequence_parallel_size: 1
  ppo_epochs: ${actor_rollout_ref.actor.ppo_epochs}
  shuffle: ${actor_rollout_ref.actor.shuffle}
  grad_clip: 1.0
  cliprange_value: 0.5

# ──────────────────────────────────────────────────────────────────────────────
# Reward
# ──────────────────────────────────────────────────────────────────────────────
reward_model:
  enable: False
  strategy: fsdp

custom_reward_function:
  path: null
  name: null

# ──────────────────────────────────────────────────────────────────────────────
# Algorithm
# ──────────────────────────────────────────────────────────────────────────────
algorithm:
  gamma: 1.0
  lam: 1.0
  adv_estimator: gae
  kl_penalty: kl
  kl_ctrl:
    type: fixed
    kl_coef: 0.001

# ──────────────────────────────────────────────────────────────────────────────
# Trainer
# ──────────────────────────────────────────────────────────────────────────────
trainer:
  total_epochs: 1
  project_name: verl-llmd
  experiment_name: gsm8k-qwen2.5-0.5b-vanilla
  logger: ['console']
  use_legacy_worker_impl: auto
  test_freq: 5
  save_freq: 10
  default_hdfs_dir: null
  default_local_dir: /tmp/verl/checkpoints
  n_gpus_per_node: 1
  nnodes: 1

# ──────────────────────────────────────────────────────────────────────────────
# Ray
# ──────────────────────────────────────────────────────────────────────────────
ray_kwargs:
  ray_init:
    runtime_env:
      env_vars:
        PYTORCH_CUDA_ALLOC_CONF: "expandable_segments:False"
